package ch02;

// enum -> 클래스의 또 다른 말이라고 생각하기
public enum Gender { // Gender 값 한정 | 열거 타입 -> 값을 한정하기 위해 사용
	MALE, // 주로 대문자로 만듦
	FEMAIL
}

// 기본 타입이 아니면 다 참조 타입이라고 생각하기
// 기본 타입 -> 정수(byte, char, short, int, long), 실수(float, double), 논리(boolean)